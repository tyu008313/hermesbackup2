Reza's HERMES_FULL_CONTEXT.md (backed up in repo /data/workspace/backup_repo) documents a REMOTE Railway server (bot MirzaPro2 at /var/www/mirza_pro, 3x-ui VPN, cloudflared tunnels, cron for www-data) — but this Hermes host is separate and does NOT host those paths. This desktop/workspace machine only holds the backup repo and context file. GitHub PAT is stored in that repo's .git/config for automated pushes.